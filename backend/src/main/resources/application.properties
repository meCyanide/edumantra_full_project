spring.application.name=portal

# Database Configuration
spring.datasource.url=jdbc:mysql://edumantradb.czuuquckaqd3.ap-south-1.rds.amazonaws.com:3306/edumantra?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=edumantra_developer
spring.datasource.password=Luchi2026July
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server configuration
server.port=8080

# Disable Spring DevTools restart to avoid runtime classloader restart issues
spring.devtools.restart.enabled=false
spring.devtools.add-properties=false

# SMTP Configuration
#spring.mail.host=${SMTP_HOST:smtp.gmail.com}
#spring.mail.port=${SMTP_PORT:587}
#spring.mail.username=${SMTP_USERNAME:}
#spring.mail.password=${SMTP_PASSWORD:}
#spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:true}
#spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:true}
#spring.mail.properties.mail.smtp.starttls.required=${SMTP_STARTTLS_REQUIRED:true}
#spring.mail.properties.mail.smtp.ssl.trust=${SMTP_SSL_TRUST:smtp.gmail.com}
#spring.mail.default-encoding=UTF-8

